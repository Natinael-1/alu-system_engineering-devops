#!/usr/bin/env bash
# Update package lists, install nginx, fix port 8080->80, and restart
sudo apt-get -y update
sudo apt-get -y install nginx
sudo sed -i 's/8080/80/g' /etc/nginx/sites-available/default
sudo service nginx restart
